xvss.1.1
; Vertex state shader
mov r0,c[95]		; Normal
mov c[95],r0        ; Write to constant register
; mov c[A0.x+32],r0	; Relative writes not allowed.
; mov oPos,r0			;Not allowed for a state shader
mov r0, v0			;Allowed
; mov r0, v1		;Not allowed for a state shader


