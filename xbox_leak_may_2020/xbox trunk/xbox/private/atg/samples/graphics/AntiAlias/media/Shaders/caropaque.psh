// blend between t0 and t1 using final combiner
// scale by diffuse color

xps.1.1

tex t0	       		// normal texture lookup in t0

mov r1, v0		// Make r1 the diffuse color

// blend modulated colors
xfc v0.a, t0, r1, zero, zero, zero, 1-zero


