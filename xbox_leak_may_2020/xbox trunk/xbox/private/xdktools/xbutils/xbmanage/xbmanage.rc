#include <windows.h>

#include <xboxverp.h>

#define VER_FILETYPE    VFT_APP
#define VER_FILESUBTYPE VFT2_UNKNOWN
#define VER_FILEDESCRIPTION_STR     "xbmanage.exe"
#define VER_INTERNALNAME_STR        "xbmanage.exe"

#include "common.ver"

#include "resource.h"

STRINGTABLE DISCARDABLE
BEGIN
    IDS_PASSWD_MISMATCH "passwords do not match"
    IDS_NOUSER "no such user |0"
    IDS_USEREXISTS "user |0 already exists"
    IDS_ACCESSDENIED "access denied"
    IDS_NOTLOCKED "security is not enabled"
    IDS_NOMANAGE "at least one user most have manage permission"
END

#include "xbfileres.h"
#include "xbfile.rc"
