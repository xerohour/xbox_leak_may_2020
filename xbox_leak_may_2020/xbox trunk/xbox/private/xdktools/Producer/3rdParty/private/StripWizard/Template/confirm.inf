The generated project will include skeleton code for a DirecMusic Producer strip editor.

It will be named $$Safe_root$$.dll.

The generated project assumes the files from the DirectMusic Producer SDK directory \samples\Shared are located in the directory ..\Shared.
The generated project assumes the files from the DirectMusic Producer SDK directory \include are located in the directoy ..\include.
The generated project assumes the files from the DirectMusic Producer SDK directory \lib are located at ..\lib.

Before the generated project can be compiled, the path to the DirectX 7.0 SDK \include directory must be added to the list of "Additional include directories."
The path to the DirectX 7.0 \lib directory must also be added to the list of "Additional library path."
